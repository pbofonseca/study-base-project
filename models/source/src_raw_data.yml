version: 2

sources:
  - name: raw_data
    config:
      enabled: true
    tables:
      - name: clientes
        columns:
          - name: id_cliente
            tests:
              - unique
              - not_null
      - name: cliente_info